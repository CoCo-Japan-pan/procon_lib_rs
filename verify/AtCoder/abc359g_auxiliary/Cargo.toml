[package]
name = "abc359g_auxiliary"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
proconio = { version = "=0.4.5", features = ["derive"] }
auxiliary_tree = { path = "../../../crates/tree/auxiliary_tree" }